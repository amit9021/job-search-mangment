# Copy this template to `.env.development`, `.env.production`, or another mode-specific file.
# Values shown here are placeholders—replace them with environment-specific secrets before running the app.

# === Shared settings ===
# TIMEZONE defines the default timezone used for scheduling logic.
TIMEZONE=UTC

# === Backend settings ===
# PORT sets the NestJS HTTP port.
PORT=3001

# JWT_SECRET secures signed tokens; use a long, random string.
JWT_SECRET=replace-with-long-random-secret
# JWT_EXPIRES_IN controls token TTL (supports formats like 12h or 7d).
JWT_EXPIRES_IN=7d
# BCRYPT_ROUNDS adjusts hashing cost (trade-off between latency and strength).
BCRYPT_ROUNDS=12

# Rate limiting (milliseconds window + max hits within the window)
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=5

# Optional OAuth experiments (leave disabled until providers are wired)
AUTH_OAUTH_ENABLED=false

# Seed bootstrap credentials for local demo data.
ADMIN_EMAIL=founder@example.com
ADMIN_PASSWORD=change_me

# BACKEND_ALLOWED_ORIGINS is a comma-separated list of allowed browser origins for CORS (e.g. https://app.example.com).
BACKEND_ALLOWED_ORIGINS=http://localhost:5173

# === Database URLs ===
# DATABASE_URL_DEV points to the development database and is automatically preferred when NODE_ENV=development.
DATABASE_URL_DEV=postgresql://user:password@localhost:5432/jobhunt_dev
# DATABASE_URL_PROD points to the production database and is automatically preferred when NODE_ENV=production.
DATABASE_URL_PROD=postgresql://user:password@prod-db:5432/jobhunt_prod
# DATABASE_URL_TEST is used for automated tests and falls back to DEV when unset.
DATABASE_URL_TEST=postgresql://user:password@localhost:5432/jobhunt_test
# DATABASE_URL is an optional fallback for tooling like Prisma CLI; set it to the matching *_DEV or *_PROD value.
DATABASE_URL=postgresql://user:password@localhost:5432/jobhunt_dev

# === Dashboard configuration ===
DASHBOARD_V1=true
DAILY_TARGET_CV=5
DAILY_TARGET_WARM=5
DASHBOARD_CACHE_TTL=15
NBA_HIGH_HEAT_THRESHOLD=3
NBA_MEDIUM_HEAT_THRESHOLD=2
NBA_FOLLOWUP_LOOKAHEAD_HOURS=48
NBA_STALE_TOUCH_DAYS=3

# === Frontend (Vite) settings ===
# FRONT_PORT sets the dev server port used by `npm run dev`.
FRONT_PORT=5173
# VITE_API_URL configures the backend API base URL (must include protocol).
VITE_API_URL=http://localhost:3001
# VITE_DASHBOARD_V1 toggles the dashboard experience.
VITE_DASHBOARD_V1=true
